// @file       widget_test.dart
// @brief      Minimal placeholder test. The real app needs Firebase + MQTT
//             init at boot, so a full pumpWidget integration test isn't
//             meaningful here without a fake harness.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
